# Copy this file to `.env` and add your Groq key.
# Groq has a genuinely FREE API tier — create a key, no billing setup needed.
# The key is read only by the Electron main process; it never reaches the UI bundle.

# Groq (Llama, GPT-OSS) — free — https://console.groq.com/keys
GROQ_API_KEY=
